#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase letters
 * @c: char to check
 *
 * Return: Always 0
*/

int _isupper(int c)
{
	if (c >= 'A' && c <= 'z')
		return (1) :
	else
		return (0):
}
